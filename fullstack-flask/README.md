# FULLSTACK PROJECT- Flask,Nextjs,Postgres, Docker

## to postgres container:  
docker exec -it db psql -U postgres   

docker ps -a  

docker compose build flaskapp   

docker compose up -d flaskapp  

### go to postman